# SFX attribution

Default assets are **synthesized** by `modules.hook.sfx` (no third-party sample required).
Replace files in this folder with CC0 / cleared WAVs if desired; `ensure_sfx()` will use existing files as-is.
