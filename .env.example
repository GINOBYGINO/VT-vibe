# Optional cookies path is via env YTDLP_COOKIES (not committed).
# Gemini / LLM keys are not used in v0.7+.

# v0.12 ASR / subtitle engines
# USE_WHISPERX_FOR_SUBTITLE=1  → Module2 uses faster-whisper; Module5 uses WhisperX
# SUBTITLE_AB_TEST5=1          → test5 writes outputs/v0.12/test5/{fast,whisperx}/
# OUTPUT_VERSION=v0.12         → export under outputs/v0.12/<alias>/
OUTPUT_VERSION=v0.12
