# v2.0 實作目標計畫

本文件是 **人工工作台（三頁）+ 精修匯出** 的產品規格與分版實作計畫。  
CLI pipeline（`pipeline.py` 模組 1–8）維持為 **粗剪引擎**；v2.0 在其上加本機 UI，不引入 Remotion。

相關入口：[`README.md`](README.md)（現行 v1.1 CLI）。

---

## 1. 一句話

貼網址跑完 1–8 粗剪 → Cursor 閘門後進待評分池 → 三維打分淘汰 → 通過的短片人工精修（取景／字幕／Hook／BGM／檔名）→ 依直播編號資料夾匯出 MP4 + 上傳備註。

---

## 2. 與現行版本的關係

| 版本 | 角色 |
|------|------|
| v1.1 CLI | 模組 1–8：下載到粗剪 `short_*_final`（含現有倒放 Hook）。**僅供 B 頁評分預覽**，不是對外成品。 |
| v2.0 | 本機工作台 + **正式片重渲**（無倒放模糊；Hook 獨立時間軸，最後一步才接上）。成品進 `outputs/v2.0/`。 |
| v1.0 / v1.1 成品 | 不覆蓋、不刪。 |

粗剪與正式片必須分開存，避免評分預覽被精修覆寫。

```text
貼 URL
  → A：job 流水號，跑 1–8（step 3 仍停等 Cursor）
  → 通過閘門的 shorts 進入 B（跨母片隨機池）
  → 評分 ≥ 10：進 C
  → 評分 < 10：先留在「前 3 部可改」；滑出這 3 部後才刪該 short 全部資料
  → C 五步草稿 → 合併渲染 → outputs/v2.0/<流水號>/
```

---

## 3. 已拍板決策（對照原 1–29）

| # | 主題 | 結論 |
|---|------|------|
| 1 | A 跑什麼 | **模組 1–8 全跑**。產出＝粗剪，只給人工評分用。 |
| 2 | B 看什麼 | 上述粗剪；且 **已通過 Cursor 篩選**。 |
| 3 | Cursor 閘門 | **保留**。閘門結論、情緒等放 B 頁側欄，**預設收起**，需要再展開。 |
| 4 | 低分刪除範圍 | **該 short 資料全部刪除**。母片 A 可顯示「已淘汰 / 總段落」。 |
| 5 | 編號 | **本機全域流水號**（從 1，刪 job 不重用）。 |
| 6 | 標題上畫面 | **不燒進畫面**。標題只用於 **最終 MP4 檔名**。 |
| 7 | 上傳備註 | 與 MP4 同資料夾一份備註：標題、原片連結、BGM 名稱。 |
| 8 | 低分／無亮點 | 維持「沒亮點就不往下做」：總分 &lt; 10 不進 C、不精修。 |
| 9 | B 池範圍 | **跨所有母片** 的待評分 short。 |
| 10 | 何時真刪 | **超出可回看的前 3 部之後** 才執行刪除。前 3 部內改分仍可救回。 |
| 11 | 三維權重 | **等權**。喜愛 + 內容 + 畫面，各 1–10，總分 30。 |
| 12 | 重點字 | `isKeyWord === true` 在文案裡用 `**重點**` 表示；寫入後自動進文字與預覽。 |
| 13 | 色調全域 vs 單句 | 切全域方案時 **單句配色被蓋掉**；**不可改重點字分布**。 |
| 14 | 彩虹隨機 | **每個一般字** 各抽一色；重骰 **不保留** `customColor`。 |
| 15 | 綜藝雙色預設 | 上 **淺藍**、下 **白**。 |
| 16 | 刪減特效 | 可關 **step6 晃動**、可關 **花字放大**（不是刪整句字幕）。 |
| 17 | 字幕時間軸 | **短片時間**（不是母片 VOD 時間）。 |
| 18 | Hook 紅點 | **短片時間碼**。 |
| 19 | Hook 預覽 | 獨立按鈕 **「預覽 Hook」**，不要點風格卡就自動跳 0s。 |
| 20 | 開場形式 | **取代倒放模糊**。正式片 **不使用** 倒放模糊。 |
| 21 | Hook 長度 | **可調 0.0–5.0 秒**（不再寫死 2.0）。 |
| 22 | Hook 時軸 | **獨立計算**；**最終步驟才合併**進正片。 |
| 23 | BGM | **共用素材庫**（專案級曲庫，非每 job 一份散檔）。 |
| 24 | 混音 | **無人聲閃避**。可選 BGM **使用哪一段**，以及 **淡入／淡出時間點與長度**。 |
| 25 | 匯出路徑 | `outputs/v2.0/<流水號>/`，檔名＝**標題**。 |
| 26 | 3 分鐘 | **設計目標**（非硬鎖超時）。 |
| 27 | Remotion | **不使用**，規格與實作皆刪除 Remotion。 |
| 28 | 載體 | 本機 **Web 工作台**（見 §8）：瀏覽器播片＋時間軸最順。 |
| 29 | 刪母片 | **A 頁可刪母片**；刪後其 shorts **不再出現於 B**（C 未完成項一併移除）。 |

補充（沿用前稿、本次未改）：

- 三維預設各 **3 分**；總分 &lt; **10** 視為無亮點（注意：3+3+3＝9，不改分就提交會走淘汰，這是刻意行為）。
- B 可回到 **前 3 部** 改分。
- C 進入子頁還原 **最後一次草稿**。
- 正式 Hook 三種風格：黃黑高光／全紅警示／強光聚焦（ffmpeg／濾鏡圖層實作，非 Remotion）。

---

## 4. 畫面架構

```text
本機工作台
├── A 自動工作進度     母片 job
├── B 待篩選影片區     跨母片 shorts（Cursor 通過且未淘汰）
└── C 影片剪輯區       評分通過、待精修／精修中
```

不另開第四頁：成品用資料夾即可；A 卡片可顯示「已匯出」。

### 4.1 A — 自動工作進度

- 輸入 YouTube 網址，入隊後跑模組 1–8；step 3 無 `review_decisions.json` 時仍停等 Cursor。
- 每成功建立一個 job：**本機流水號 +1**（持久化，例如 `studio/serial.json`）。
- 卡片：編號、標題／縮圖、直播日期、目前步驟 1–8、失敗重試、`總片段 x | 已評分 n/m | 待剪輯 y/z | 已淘汰 k`。
- **刪除母片**：二次確認後刪 job 工作目錄與該編號下未匯出草稿；B 立即不顯示該母片 shorts。已寫入 `outputs/v2.0/<編號>/` 的成品是否保留：預設 **保留成品、只刪工作檔**（避免誤刪已上傳素材）。

狀態建議：`排隊 → 處理中 → 待 Cursor → 跑 4–8 → 待評分 → 部分待剪輯／已完成 → 已刪除`。

### 4.2 B — 待篩選影片區

- 直式全螢幕播放，**向下滑動**進下一支（同時送出目前三維分與備註；跨母片隨機）。向上滑可回到 **最近 3 支** 改分。
- 分數面板在畫面 **中右上**（喜愛／內容／畫面 + 總分 + **備註**）。
- Cursor／模組 3 資訊在 **中右下**，預設收起。
- 總分 &lt; 10：標記淘汰，**仍留在前 3 部**；一旦被擠出前 3，**刪除該 short 全部資料**，母片「已淘汰」+1。

不提供「只看某一母片」篩選（拍板為跨母片）；若之後痛再加，不算 v2.0 必做。

### 4.3 C — 影片剪輯區

**列表卡片：** 三維分、總分、截圖、直播日期、流水號。

**子頁 stepper（可來回，autosave）：**

1. 定位人臉（ROI）＋ **母片時間窗 ±60s 與刪減片段**（v2.0.2）。
2. 修文字與特效開關 + 雙時間軸 + Hook 軌（短片時間）。
3. Hook 風格與長度（獨立時軸；預覽用專用按鈕）。
4. BGM（素材庫、選段、淡入淡出、音量、無 BGM）。
5. 定標題 → 合併渲染 → 寫入編號資料夾 + 上傳備註。

---

## 5. C 步細節（實作契約）

### 5.1 取景與母片時間窗（v2.0.2 實作）

- **取景來源是 `raw_video.mp4`（無字幕／特效／Hook）**，在橫式畫面上拖 9:16 黃框；正式片用新版面，**不用**舊 letterbox 字幕帶。
- 列表封面取 **該段第 5 秒**（母片時間窗內）。
- 可 **向前、向後各最多 60 秒**（不可超出整場直播），並可刪減多段。
- 草稿：`trim` + `roi: { cx, cy, zoom }`。

### 5.2 字幕與特效（v2.0.3）

- 字幕位置改為可調（X／Y 或對齊預設），疊在新 9:16 取景上，不沿用舊 `SUBTITLE_Y_RATIO` 帶。

**左：** 短片預覽。  
**右 Tab：** `[字幕列表]` `[字幕色調]`（Hook 設定可在步驟 3，不必塞滿同一欄）。  
**下：** 軌 1 影片 playhead；軌 2 字幕 block（拖本體改 start、拖邊改 duration）；與手風琴時間 **雙向同步**。時間基準＝**短片秒數**。

手風琴標題：`00:01.20 - 00:03.50`。

- 文字框：`**詞**` ↔ `isKeyWord`；改其一，另一邊與預覽都更新。
- 點字塊：切換重點，或 Color Picker → `customColor`。
- 時間精度 0.01s。

**色調方案**

| ID | 名稱 | 一般字 | 重點字 |
|----|------|--------|--------|
| gold | 預設金經典 | `#FFFFFF` | `#FFD700` + 放大 1.25x + 粗黑邊 |
| rainbow | 隨機彩虹 | 每字隨機：粉紅／螢光綠／水藍／天藍 | 放大；色可隨機；手動覆寫可被重骰清掉 |
| split | 綜藝雙色 | 上 50% 淺藍、下 50% 白，硬切無漸層 | `#FF0000` + 粗黑邊 |

切換全域方案：覆蓋單句顏色覆寫；**不重算、不改** 哪些字是重點。

**特效開關（刪減）：** 全片或逐句可關晃動、可關花字放大。

預覽與出片皆用 **ffmpeg + ASS／濾鏡**，不用 Remotion、不用瀏覽器當最終渲染器。

### 5.3 Hook

```text
timestamp: number | null   # 短片時間碼；null 或刪紅點 = 無 hook
duration:  0.0–5.0
enabled:   boolean
styleType: YELLOW_BLACK_CONTRAST | FULL_RED | HIGHLIGHT_GLOW
```

- 紅點 + 自紅點向右 `duration` 秒半透明紅條（評分／對點用）。
- 拖曳、±0.1s、[將目前播放頭設為爆點]、可刪。
- **[預覽 Hook]**：依風格把「獨立 Hook 段」播一次（開場濾鏡），不自動搶走正在對點的 playhead。
- **獨立時間軸：** 編輯時正片仍從短片 0s 播；Hook 段另算。步驟 5 才：`Hook 段 + 正片` 接成一條 MP4。
- `enabled === false` 或無紅點：正式片不接 Hook，長度＝正片。
- 三種風格用 ffmpeg 濾鏡近似（灰階高對比＋黃字／紅疊層／暗角＋glow），**無倒放、無倒放模糊**。

### 5.4 BGM 素材庫

- 專案目錄例如 `assets/bgm/` + 索引（顯示名、檔名）。
- 選項含 **無 BGM**。
- 可調：音量；BGM 檔內 **in/out 點**（用哪一段）；淡入起點與長度、淡出起點與長度。
- 無人聲 sidechain。

### 5.5 標題與輸出

- 標題：檔名（非法字元替換）；不燒畫面。
- 目錄：`outputs/v2.0/<流水號>/`
- 檔案至少：
  - `<標題>.mp4`
  - `<標題>.upload.txt` 或 `.json`：標題、原片 URL、BGM 名稱（無 BGM 則寫「無」）
- 建議另存 `edit_state.json`（可重開 C），非上傳必備。

---

## 6. 建議資料形狀（實作時可微調欄位名）

- `studio/serial.json`：`next_id`、已用編號。
- `jobs/<job>/job.json`：既有步驟 + `studio_serial`、淘汰數、段落總數。
- `jobs/<job>/studio/clips/<short_id>.json`：評分、淘汰標記、C 草稿（字幕、ROI、hook、bgm、title）。
- 粗剪：沿用 08／studio9 預覽檔，B 只讀。
- 正式片：只寫 `outputs/v2.0/`。

---

## 7. 載體（#28）

**採用：本機 Web 工作台**

- 後端：現有 Python（FastAPI 或同等）包 pipeline、job 狀態、刪除、渲染佇列。
- 前端：單一 SPA（Vue 或 React 擇一即可），`<video>` + 自製時間軸。
- 啟動：`python -m studio` 開瀏覽器 `127.0.0.1`。

不選 Remotion；不強制 Electron（本機資料夾權限由後端讀寫即可）。

---

## 8. 分版實作（慢慢做）

每個里程碑應可獨立使用或至少可手動驗收；做完再進下一版。  
版本號：工作台用 **v2.0.x**；pipeline 粗剪仍叫 v1.1 引擎直到正式片渲染接上。

| 里程碑 | 名稱 | 範圍 | 完成定義 |
|--------|------|------|----------|
| **v2.0.0** | 工作台殼 + A | 本機網頁骨架；貼 URL 觸發 1–8；步驟進度；流水號；A 刪母片；已淘汰/總段落（淘汰計數可先接假資料或 Cursor 拒絕數） | **已實作** |
| **v2.0.1** | B 評分池 | 跨母片隨機粗剪；三維等權；側欄收起閘門/情緒；前 3 部可改；滑出後低分全刪 | **已實作** |
| **v2.0.2** | C 列表 + 取景 | 卡片：分數／截圖／日期／編號；ROI 拖放縮放；母片 ±60s 與刪減；草稿存檔 | **已實作** |
| **v2.0.3** | 字幕編輯 | 新手風琴／色調／時間軸；字幕位置可調；正式片用新版面 | **已實作**（預覽字幕按鈕；不自動轉檔） |
| **v2.0.4** | Hook | 獨立時軸、紅點、0–5s、三風格、預覽按鈕、無倒放；最小 concat | **已實作**（預覽接片寫入 studio/preview，非正式包） |
| **v2.0.5** | BGM 素材庫 | 共用庫、無 BGM、選段、淡入淡出、音量 | 預覽能聽到選段；出片燒進去 |
| **v2.0.6** | 正式匯出 | 標題檔名、`outputs/v2.0/<編號>/`、上傳備註、合併 Hook+正片+字幕+特效+BGM | 該編號資料夾可直接當上傳包 |

**刻意延後（v2.1+，非本計畫必做）**

- 臉偵測模型升級（A/C 取景框先人工）。
- B 依母片篩選、人聲閃避、標題燒畫面、Electron。
- 用瀏覽器當最終渲染器。

**建議開發順序理由：** 先 A/B 才能每天用「貼片＋刷分」；C 從取景、字幕（最耗時）再到 Hook/BGM/出片，避免一開始卡在渲染。

---

## 9. 驗收清單（全 v2.0 做完時）

- [ ] 新 URL → 流水號遞增；刪號不重用。
- [ ] A 可刪母片，B 立刻沒有該場 shorts。
- [ ] B 只出現 Cursor keep 後的粗剪；側欄預設收起。
- [ ] 等權三維；&lt;10 分在離開前 3 部後檔案級刪除；A 顯示已淘汰/總段落。
- [ ] 正式 MP4 無倒放模糊；Hook 可關、可 0–5s、風格可選；預覽不搶對點。
- [ ] 字幕短片時軸；全域色調蓋單句色、不改重點分布；彩虹每字隨機、重骰清自訂色；雙色淺藍/白。
- [ ] 可關晃動與花字放大。
- [ ] BGM 來自素材庫或無；可選段與淡入淡出。
- [ ] `outputs/v2.0/<編號>/<標題>.mp4` + 備註（標題、原片連結、BGM 名）。
- [ ] 畫面無標題燒錄。

---

## 10. 非目標（v2.0 不做）

- Remotion / React-video 渲染。
- 用 LLM 取代 Cursor 閘門。
- 自動閃避人聲、自動日更權重。
- 覆蓋 `outputs/v1.0`、`outputs/v1.1`。

---

## 11. 下一步（開始 v2.0.0 時）

1. 定前端框架（建議 Vue 或 React 擇一，專案內只留一套）。
2. 定 API：`POST /jobs`、`GET /jobs`、`DELETE /jobs/{id}`、`GET /queue`（B 池）。
3. 流水號與「刪母片 → B 過濾」先做實、渲染後做。
4. 粗剪路徑與 B 播放檔對齊現有 `08_hook`／`09_studio9` 產物，避免再複製一份巨型 mp4。
